userChoice = prompt("rock paper or scissors?");
console.log(userChoice);

randomNumber = Math.random();
console.log(randomNumber);

function conversionToGame(randomNumber){
  if(randomNumber<0.4){
    return "paper";
  }if else (randomNumber>=0.4)
}